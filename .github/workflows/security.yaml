name: Security Scan

on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  trivy:
    name: 🛡️ Vulnerability Scan (Trivy)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: 🏁 Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      # --- TTL Cache Logic ---
      # This generates a string like '2026-01-30'.
      # By including this in the key, we force a fresh cache entry every day.
      - name: 🗓️ Get Date
        id: get-date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: 💾 Cache Trivy DB (Daily TTL)
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.0
        with:
          path: .cache/trivy
          # Key includes the date for TTL, Restore-keys allow grabbing yesterday's DB to fetch only diffs.
          key: ${{ runner.os }}-trivy-${{ steps.get-date.outputs.date }}
          restore-keys: |
            ${{ runner.os }}-trivy-

      - name: 🔍 Run Trivy scan
        uses: aquasecurity/trivy-action@b6643a29fecd7f34b3597bc6acb0a98b03d33ff8 # 0.33.1
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "CRITICAL,HIGH"
          exit-code: "0"
          cache-dir: .cache/trivy

      - name: 📤 Upload results to GitHub Security
        uses: github/codeql-action/upload-sarif@cf1bb45a277cb3c205638b2cd5c984db1c46a412 # v4.31.7
        if: always()
        with:
          sarif_file: "trivy-results.sarif"

  trufflehog:
    name: 🕵️ Secret Scan (TruffleHog)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      issues: write
      pull-requests: write

    steps:
      - name: 🏁 Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 🕵️ Run TruffleHog OSS
        uses: trufflesecurity/trufflehog@7f4e37db2d928c18ddd7ddf0604f8f7d1f5793ec # v3.93.0
        with:
          extra_args: --results=verified,unknown
